/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    // autoprefixer: {}, // Autoprefixer added for the adding of vendor prefixes for a better browser compatibility
  },
};

export default config;
